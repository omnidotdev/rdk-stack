# RDK Services Configuration
# Copy to services.yaml and customize
#
# Format:
#   services:
#     - name:
#         repo: repository URL (required)
#         path: local path (optional, defaults to services/name)
#         metarepo: true if this is a metarepo with services/ subdirectory

services:
  # Core library
  - rdk-framework:
      repo: git@github.com:omnidotdev/rdk-framework#master

  # Website
  - rdk-site:
      repo: git@github.com:omnidotdev/rdk-site#master
